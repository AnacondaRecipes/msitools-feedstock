anaconda_config_version: 1
upstream_sources:
  - gitlab:
      identifier: GNOME/msitools
      host: gitlab.gnome.org
      use_max_tag: true
      include_regex: ^v[\d.]+$
      from_pattern: ^v(.+)$
      to_pattern: \1
